# Notes (Hardened)

A note-taking skill with protected memory keys.

## Usage

Same as vulnerable version:
```
/note <key> <value>
```

## Security Features

- Protected system keys cannot be modified
- Audit log of all changes
- Blocks attempts to poison memory
